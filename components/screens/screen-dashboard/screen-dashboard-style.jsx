import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    wrapperProduct: {
        flexDirection: 'row',
        flexWrap: 'wrap',
        rowGap: 30,
        columnGap: 15,
        paddingTop: 15,
        paddingBottom: 15,
    }
})

export default styles;